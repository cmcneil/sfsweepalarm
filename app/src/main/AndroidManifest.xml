<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" /> &lt;!&ndash; Required for 29+. &ndash;&gt; -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".SFSweepAlertApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SFSweepAlert"
        tools:targetApi="31">
        <receiver
            android:name="us.groundstate.sfsweepalert.background.AlertReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <receiver
            android:name="us.groundstate.sfsweepalert.background.TransitionsReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.google.android.gms.permission.ACTIVITY_RECOGNITION" /> <!-- <service -->
        <!-- android:name=".DetectedActivityService" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true" /> -->
        <!-- <receiver -->
        <!-- android:name=".CarLocationReciever" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true"> -->
        <!-- </receiver> -->
        <activity
            android:name="us.groundstate.sfsweepalert.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SFSweepAlert">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>